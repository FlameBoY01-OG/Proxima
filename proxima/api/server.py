"""FastAPI app (Phase 4).

Endpoints: create-collection, upsert, search (+ optional metadata filter),
delete, metrics (latency / recall / QPS / vector count), list-collections,
and (Phase 4.5) /demo/seed + /demo/reset. Pydantic models + CORS for the UI.
"""
