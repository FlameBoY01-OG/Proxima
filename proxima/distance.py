"""Distance / similarity functions over numpy arrays.

Phase 1 fills this in: cosine, L2 (Euclidean), and dot product, all
numpy-vectorized so we can score one query against many vectors at once.
"""
