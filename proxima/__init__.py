"""Proxima — a hand-rolled vector database.

A durable SQLite persistence layer + a from-scratch HNSW index, wrapped in a
FastAPI service with a React UI. See BUILD plan / README for the full story.
"""

__version__ = "0.0.0"
