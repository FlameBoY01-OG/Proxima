"""Demo dataset + seed/reset logic (Phase 4.5).

A reusable sample dataset (~50-100 media/anime titles, each with a small
embedding clustered by genre + metadata {genre, year, studio}) plus the logic
to insert it and to clear it. Clear clusters make the 2D UI map land.
Shared by scripts/seed.py and the /demo/seed + /demo/reset API endpoints.
"""
