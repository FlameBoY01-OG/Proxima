# We pin as we add so each phase stays reproducible.
# Later phases append: fastapi, uvicorn, pydantic, scikit-learn, etc.

# --- Phase 1: brute-force core ---
numpy>=1.26,<3.0
pytest>=8.0,<10.0

# --- Phase 4: FastAPI service ---
fastapi>=0.110,<1.0
uvicorn>=0.27,<1.0
httpx>=0.27,<1.0          # used by FastAPI's TestClient in tests
