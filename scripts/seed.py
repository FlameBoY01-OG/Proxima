"""CLI for demo data (Phase 4.5).

    python -m scripts.seed --seed     # insert the demo dataset
    python -m scripts.seed --reset    # clear it

Thin wrapper around proxima/demo.py so the CLI and the API share one code path.
"""
